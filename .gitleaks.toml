# Gitleaks config — extends the built-in ruleset.
# Docs: https://github.com/gitleaks/gitleaks

[extend]
useDefault = true

[allowlist]
description = "Global allowlist for config and example values"

paths = [
  '''\.gitleaks\.toml$''',
  '''(^|/)\.env\.example$''',
]
